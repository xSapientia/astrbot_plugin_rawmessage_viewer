{
  "enable_auto_insert": {
    "description": "自动插入原始消息",
    "type": "bool",
    "default": true,
    "hint": "是否在每条消息前自动插入原始消息内容"
  },
  "enable_logging": {
    "description": "启用日志记录",
    "type": "bool",
    "default": true,
    "hint": "是否在后台记录插入原始消息后的完整内容"
  },
  "delete_config_on_uninstall": {
    "description": "卸载时删除配置",
    "type": "bool",
    "default": false,
    "hint": "卸载插件时是否删除配置文件和数据"
  }
}
