{
  "enable_plugin": {
    "description": "插件总开关",
    "type": "bool",
    "default": true
  },
  "enable_llm_prefix": {
    "description": "是否在 LLM 请求前添加 RawMessage 信息",
    "type": "bool",
    "default": true,
    "hint": "开启后会在每次 LLM 请求前添加原始消息信息"
  },
  "prefix_format": {
    "description": "LLM Prefix 格式模板",
    "type": "text",
    "default": "【消息元数据】\n发送者: {sender_name} ({sender_id})\n群组: {group_name} ({group_id})\n时间: {time}\n消息类型: {message_type}\n原始内容: {raw_content}\n---\n",
    "hint": "支持的变量: {sender_name}, {sender_id}, {group_name}, {group_id}, {time}, {message_type}, {raw_content}"
  },
  "show_raw_json": {
    "description": "查看原始消息时是否显示完整 JSON",
    "type": "bool",
    "default": false,
    "hint": "开启后会显示完整的 RawMessage JSON 数据"
  },
  "max_prefix_length": {
    "description": "Prefix 最大长度限制",
    "type": "int",
    "default": 500,
    "hint": "超过此长度的内容会被截断，设置为 0 表示不限制"
  },
  "delete_data_on_uninstall": {
    "description": "卸载插件时是否删除数据",
    "type": "bool",
    "default": false,
    "hint": "包括配置文件和缓存数据"
  }
}
